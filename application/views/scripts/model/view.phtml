<?php
    $this->title = $this->model->names[0]->name;
    $this->headTitle($this->model->names[0]->name);
?>

<?php echo $this->model->names[0]->name; ?>

<div>
    <a href="<?php echo $this->url(array('controller'=>'model', 'action'=>'edit', 'id' => $this->model->ID));?>">Edit</a>
</div>

<?php if ($this->model->photosets): ?>
    <ul id="photosets">
        <?php echo $this->partialLoop('photosetPartial.phtml', $this->model->photosets); ?>
    </ul>
<?php endif; ?>